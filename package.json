{
  "name": "testcase-repo-alice",
  "version": "1.0.0",
  "main": "dist/algorithm.test.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/tests/algorithm.test.js",
    "test:all": "npm run build && node dist/tests/algorithm.test.js && node dist/tests/performance.test.js && node dist/tests/integration.test.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
